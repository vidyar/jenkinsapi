# This YML file was generated by the Shippable Jenkins migrator wizard
# The generator makes some assumptions about your project, so please 
# verify the contents of this file before proceeding

language: python

python:
 - 2.7

#build_image: shippableimages/ubuntu1404_python

script: 
 #- PATH=$WORKSPACE/ve/bin:/usr/local/bin:$PATH
 #- virtualenv ve
 #- . ve/bin/activate
 - sudo apt-get install -y python-dev
 - sudo easy_install -U pip
 - sudo pip install nose mock coverage pep8 pylint
 - sudo python setup.py develop
 - pylint --rcfile=pylintrc jenkinsapi/*.py --disable R0912
 - pep8 --ignore=E501 jenkinsapi/*.py
 - mkdir -p vidyar/testresults vidyar/codecoverage
 - nosetests jenkinsapi_tests --with-coverage --cover-package=jenkinsapi  --with-xunit --xunit-file=vidyar/testresults/nosetests.xml

 - coverage xml -o vidyar/codecoverage/coverage.xml